import { useState } from 'react'
import CustomerLayout from '../components/CustomerLayout.jsx'
function Settings() { const [settings, setSettings] = useState({ booking: true, promotions: false, reminder: true }); const toggle = (key) => setSettings((current) => ({ ...current, [key]: !current[key] })); const options = [['booking', 'Booking updates', 'Get notified when a provider accepts or updates a booking.'], ['promotions', 'Offers and promotions', 'Receive recommendations and special offers from Local Services.'], ['reminder', 'Booking reminders', 'Receive a reminder before your scheduled appointment.']]; return <CustomerLayout title="Settings" subtitle="Manage your notifications and account preferences."><section className="panel settings-panel"><h2>Notification preferences</h2><p>Choose what you want to hear about.</p>{options.map(([key, label, description]) => <label className="setting-row" key={key}><span><strong>{label}</strong><small>{description}</small></span><input type="checkbox" checked={settings[key]} onChange={() => toggle(key)} /></label>)}</section></CustomerLayout> }
export default Settings