import { useState } from 'react'
import CustomerLayout from '../components/CustomerLayout.jsx'
const initial = [{ id: 1, title: 'Booking confirmed', text: 'Priya Home Care confirmed your booking for 16 July.', time: '10 minutes ago', unread: true }, { id: 2, title: 'Payment received', text: 'Your payment of $120.00 was received successfully.', time: '2 days ago', unread: true }, { id: 3, title: 'Review reminder', text: 'How was your AC servicing appointment?', time: '12 days ago', unread: false }]
function Notifications() { const [notifications, setNotifications] = useState(initial); const readAll = () => setNotifications((items) => items.map((item) => ({ ...item, unread: false }))); return <CustomerLayout title="Notifications" subtitle="Stay up to date with your services and account."><section className="panel"><div className="panel-heading"><div><h2>Recent notifications</h2><p>{notifications.filter((n) => n.unread).length} unread notifications</p></div><button className="text-button" onClick={readAll}>Mark all as read</button></div><div className="notification-list">{notifications.map((n) => <article className={`notification-item ${n.unread ? 'unread' : ''}`} key={n.id}><span>??</span><div><strong>{n.title}</strong><p>{n.text}</p><small>{n.time}</small></div></article>)}</div></section></CustomerLayout> }
export default Notifications