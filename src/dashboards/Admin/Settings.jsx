import { useState } from 'react'
import AdminLayout from '../components/AdminLayout.jsx'
function Settings() { const [settings, setSettings] = useState({ registration: true, email: true, reviews: false }); const toggle = (key) => setSettings((current) => ({ ...current, [key]: !current[key] })); return <AdminLayout title="Settings" subtitle="Configure platform preferences and administration options."><section className="panel settings-panel"><h2>Platform preferences</h2><p>Control the main behaviour of your marketplace.</p>{[['registration', 'Allow new customer registrations', 'Customers can create accounts directly.'], ['email', 'Email notifications', 'Send booking and payment updates by email.'], ['reviews', 'Review moderation', 'Require administrator approval before publishing reviews.']].map(([key,label,description]) => <label className="setting-row" key={key}><span><strong>{label}</strong><small>{description}</small></span><input type="checkbox" checked={settings[key]} onChange={() => toggle(key)} /></label>)}</section></AdminLayout> }
export default Settings