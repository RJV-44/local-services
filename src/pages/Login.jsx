import { useState } from 'react'
import Navbar from '../components/Navbar.jsx'
import Footer from '../components/Footer.jsx'
function Login() { const [submitted, setSubmitted] = useState(false); return <div className="public-page"><Navbar onRegister={() => { window.location.hash = '#register' }} /><main className="auth-page"><form className="auth-card" onSubmit={(event) => { event.preventDefault(); setSubmitted(true) }}><a href="#home" className="back-link">? Back to home</a><h1>Welcome back</h1><p>Log in to manage your bookings and account.</p><label>Email address<input type="email" placeholder="you@example.com" required /></label><label>Password<input type="password" placeholder="••••••••" required /></label><div className="auth-row"><label className="checkbox-label"><input type="checkbox" /> Remember me</label><a href="#login">Forgot password?</a></div><button className="primary-button" type="submit">Log in</button>{submitted && <small className="form-success">Demo login submitted. Choose a dashboard from the menu.</small>}<p className="auth-switch">New to Local Services? <a href="#register">Create an account</a></p></form></main><Footer /></div> }
export default Login