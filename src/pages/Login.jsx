import { useState } from 'react'
import Navbar from '../components/Navbar.jsx'
import Footer from '../components/Footer.jsx'
import { useAuth } from '../context/AuthContext'
function Login() { const { login } = useAuth(); const [submitted, setSubmitted] = useState(false); const [role, setRole] = useState('Customer'); return <div className="public-page"><Navbar onRegister={() => { window.location.hash = '#register' }} /><main className="auth-page"><form className="auth-card" onSubmit={(event) => { event.preventDefault(); setSubmitted(true); const roleKey = role === 'Provider' ? 'provider' : 'customer'; login({ name: role === 'Provider' ? 'Priya Sharma' : 'Sophia Patel', email: 'user@example.com', role: roleKey }); setTimeout(() => { window.location.hash = role === 'Provider' ? '#provider-dashboard' : '#customer-dashboard' }, 800) }}><a href="#home" className="back-link">? Back to home</a><h1>Welcome back</h1><p>Log in to manage your bookings and account.</p><label>Email address<input type="email" placeholder="you@example.com" required /></label><label>Password<input type="password" placeholder="••••••••" required /></label><label>I am a<select value={role} onChange={(e) => setRole(e.target.value)}><option>Customer</option><option>Provider</option></select></label><div className="auth-row"><label className="checkbox-label"><input type="checkbox" /> Remember me</label><a href="#login">Forgot password?</a></div><button className="primary-button" type="submit">Log in</button>{submitted && <small className="form-success">Logging you in as {role}...</small>}<p className="auth-switch">New to Local Services? <a href="#register">Create an account</a></p><p className="auth-switch"><a href="#admin-login">Admin login ?</a></p></form></main><Footer /></div> }
export default Login