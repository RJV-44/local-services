import { useState } from 'react'
import Navbar from '../components/Navbar.jsx'
import Footer from '../components/Footer.jsx'
import { useAuth } from '../context/AuthContext'
function Register() { const { login } = useAuth(); const [submitted, setSubmitted] = useState(false); const [role, setRole] = useState('Customer'); return <div className="public-page"><Navbar onLogin={() => { window.location.hash = '#login' }} /><main className="auth-page"><form className="auth-card" onSubmit={(event) => { event.preventDefault(); setSubmitted(true); const roleKey = role === 'Provider' ? 'provider' : 'customer'; login({ name: 'New Member', email: 'new@example.com', role: roleKey }); setTimeout(() => { window.location.hash = role === 'Provider' ? '#provider-dashboard' : '#customer-dashboard' }, 800) }}><a href="#home" className="back-link">? Back to home</a><h1>Create your account</h1><p>Join thousands of people who book trusted local help.</p><label>Full name<input placeholder="Your full name" required /></label><label>Email address<input type="email" placeholder="you@example.com" required /></label><label>Password<input type="password" placeholder="Create a password" required /></label><label>I want to join as<select value={role} onChange={(e) => setRole(e.target.value)}><option>Customer</option><option>Service provider</option></select></label><label className="checkbox-label"><input type="checkbox" required /> I agree to the terms and privacy policy.</label><button className="primary-button" type="submit">Create account</button>{submitted && <small className="form-success">Account created as {role}. Redirecting to your dashboard...</small>}<p className="auth-switch">Already have an account? <a href="#login">Log in</a></p></form></main><Footer /></div> }
export default Register