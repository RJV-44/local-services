import { useState } from 'react'
import Navbar from '../components/Navbar.jsx'
import Footer from '../components/Footer.jsx'
import { useAuth } from '../context/AuthContext'
function AdminLogin() { const { login } = useAuth(); const [submitted, setSubmitted] = useState(false); return <div className="public-page"><Navbar /><main className="auth-page"><form className="auth-card admin-auth-card" onSubmit={(event) => { event.preventDefault(); setSubmitted(true); login({ name: 'Admin', email: 'admin@localservices.com', role: 'admin' }); setTimeout(() => { window.location.hash = '#admin-dashboard' }, 800) }}><a href="#home" className="back-link">? Back to home</a><div className="admin-badge">? Admin</div><h1>Admin login</h1><p>Sign in to manage platform operations.</p><label>Admin email<input type="email" placeholder="admin@localservices.com" required /></label><label>Password<input type="password" placeholder="Enter admin password" required /></label><label>Security code<input type="text" placeholder="Enter 6-digit security code" maxLength="6" required /></label><button className="primary-button admin-button" type="submit">Sign in as admin</button>{submitted && <small className="form-success">Admin authenticated. Redirecting to admin dashboard...</small>}<p className="auth-switch"><a href="#login">Back to user login</a></p></form></main><Footer /></div> }
export default AdminLogin